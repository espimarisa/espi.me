<div>
  <figure class="{{ include.class }}">
    <img width="{{ include.width }}" height="{{ include.height }}" src="{{ include.src }}" alt="{{ include.alt }}" align="{{ include.align }}">
    <figcaption>{{ include.caption }}</figcaption>
  </figure>
</div>
