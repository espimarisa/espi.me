---
layout: default.liquid
title: mari's blog
description: ramblings from an unhinged dork
---

{% comment %} Header {% endcomment %}
<header>
	<h1 class="has-no-margin">{{ title }}</h1>
	<h2 class="has-no-margin">{{ description }}</h2>
</header>

{% comment %} Blog {% endcomment %}
<ul>
	{%- for post in collections.posts %}
		<li>
			<a href="{{ post.url }}">{{ post.data.title }}</a>
			<span>({{ post.date | postDate }})</span>
		</li>
	{%- endfor %}
</ul>

{% comment %} Footer {% endcomment %}
<footer>
	<ul>
		<li><a href="/">home</a></li>
	</ul>
</footer>
