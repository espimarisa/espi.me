---
layout: default.liquid
title: "Espi Marisa"
description: "Silly enby. Blind developer and activist."
bluesky: "espi.me"
blueskydid: "did:plc:tuihqhadazqooxe5fhw6dhzk"
discordHandle: "espimarisa"
discordID: "647269760782041133"
email: "contact [at] domain [dot] tld"
emailURL: "contact@espi.me?subject=espi.me"
github: "espimarisa"
fediHandle: "@espi@uwu.social"
fediURL: "https://uwu.social/@espi"
pronouns: "they/she"
matrixHandle: "@espimarisa:matrix.org"
signalHandle: "espimarisa.01"
signalURL: "https://signal.me/#eu/0rRdxJjNEyP9MRGqdRo7p0iiMR2dO0tsTKMskW2_YeO5aBb5FVpOKKCBDcfwIfhN"
telegram: "espimarisa"
twitterx: "espidotme"
---

{% comment %} Header {% endcomment %}
<header>
  <h1>{{ title }}</h1>
  <h3>{{ description }}</h3>

  <nav>
    {% comment %} Links {% endcomment %}
    <ul>
      <li>
        <a
          aria-label="Bluesky: {{ bluesky }}"
          href="https://bsky.app/profile/{{ blueskydid }}"
          target="_blank"
          title="Bluesky: {{ bluesky }}">Bluesky</a>

        <span>@{{ bluesky }}</span>
      </li>
      <li>
        <a
          aria-label="GitHub: {{ github }}"
          href="https://github.com/{{ github }}"
          target="_blank"
          title="GitHub: {{ github }}">GitHub</a>

        <span>@{{ github }}</span>
      </li>
      <li>
        <a
          aria-label="Discord: {{ discordHandle }}"
          href="https://discord.com/users/{{ discordID }}"
          target="_blank"
          title="Discord: {{ discordHandle }}">Discord</a>

        <span>@{{ discordHandle }}</span>
      </li>
      <li>
        <a
          aria-label="X: {{ twitterx }}"
          href="https://x.com/{{ twitterx }}"
          target="_blank"
          title="Twitter/X: {{ twitterx }}">Twitter/X</a>

        <span>@{{ twitterx }}</span>
      </li>
      <li>
        <a
          aria-label="Telegram: {{ telegram }}"
          href="https://t.me/{{ telegram }}"
          target="_blank"
          title="Telegram: {{ telegram }}">Telegram</a>

        <span>@{{ telegram }}</span>
      </li>
      <li>
        <a
          aria-label="Signal: {{ signalHandle }}"
          href="{{ signalURL }}"
          target="_blank"
          title="Signal: {{ signalHandle }}">Signal</a>

        <span>@{{ signalHandle }}</span>
      </li>
      <li>
        <a
          aria-label="Fediverse: {{ fediHandle }}"
          href="{{ fediURL }}"
          target="_blank"
          title="Fediverse: {{ fediHandle }}">Fediverse</a>

        <span>{{ fediHandle }}</span>
      </li>
      <li>
        <a
          aria-label="Matrix: {{ matrixHandle }}"
          href="https://matrix.to/#/{{ matrixHandle }}"
          target="_blank"
          title="Matrix: {{ matrixHandle }}">Matrix</a>

        <span>{{ matrixHandle }}</span>
      </li>
      <li>
        <a
          aria-label="Email: {{ email }}"
          href="mailto:{{ emailURL }}"
          target="_blank"
          title="Email: {{ email }}">Email</a>

        <span>{{ email }}</span>
      </li>
    </ul>
  </nav>
</header>

<main>
  {% comment %} About {% endcomment %}
  <section id="about">
    <h2 class="inline">Hi, my name's Espi!</h2>
    <small>({{ pronouns }})</small>
    <p>
      I'm a blind developer, tinkerer, and activist living in Huntsville, Alabama.
    </p>

    <p>I primarily work with Bun, Svelte, TypeScript, and various web frameworks.</p>
    <p>Currently, I'm a Computer Science student attending Auburn University online.</p>

    <br/>

    <p>In my spare time, I enjoy watching anime, researching severe weather, messing with electronics, playing the guitar, all things 
      Pokémon, and exploring my home of Appalachia and the deep south.</p>

    <br/>

    <p>I volunteer for the
      <a
        href="https://altrac.works"
        title="ALTRAC"
        target="_blank">Alabama Transgender Rights Action Coalition</a>
      to fight discriminatory legislation.
    </p>

    <p>
      I also help maintain a collection of
      <a
        href="https://altgo.us"
        title="ALTGO"
        target="_blank">local resources and legal guides</a>
      for queer and trans Alabamians.
    </p>

  </section>

  {% comment %} Projects {% endcomment %}
  <section id="projects">
    <h2>Projects</h2>
    <p>
      Some notable things that I've made or contributed to:
    </p>
    <div class="row">
      {% for project in collections.projects reversed %}
        <div class="box column">
          <a
            href="{{ project.data.url }}"
            title="{{ project.data.title }}"
            target="_blank">
            <p class="title">{{ project.data.title }}</p>
            <p>{{ project.data.description }}</p>
            <p>
              <small>
                <i>
                  {{ project.data.technologies }}
                </i>
              </small>
            </p>
          </a>
        </div>
      {% endfor %}
    </div>
  </section>

  {% comment %} Webring {% endcomment %}
  <section id="webring">
    {% for ring in collections.webring %}
      {% comment %} only append a to buttons with links {% endcomment %}
      {% if ring.data.link %}
        <a
          href="{{ ring.data.link }}"
          title="{{ ring.data.name }}"
          target="_blank">
      {% endif %}

      {% comment %} Nasty hotlink support {% endcomment %}
      {% if ring.data.hotlink %}
        <img
          src="{{ ring.data.hotlink }}"
          alt="{{ ring.data.title }}"
          title="{{ ring.data.title }}"
          width="88px"
          height="31px">
      {% else %}
        <img
          src="/img/webring/{{ ring.data.file | url }}"
          alt="{{ ring.data.title }}"
          title="{{ ring.data.title }}"
          width="88px"
          height="31px">
      {% endif %}

      {% comment %} nasty inline trick to avoid duplication {% endcomment %}
      {% if ring.data.link %}
        </a>
      {% endif %}
    {% endfor %}
  </section>
</main>
