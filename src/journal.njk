---
title: Journal
description: Writings from a cursed, gifted, and unique being.
layout: post.njk
---

{% for post in collections.posts %}
	<a href="{{ post.url }}">{{ post.data.title }}</a>
	<p>{{ post.data.description }}</p>
	<p>{{ post.data.date.toLocaleDateString() }}</p>
{% endfor %}
